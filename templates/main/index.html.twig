{% extends 'base.html.twig' %}


{% block body %}

        <h1>Les Argonautes</h1>

                <td>
                    <a class="btn btn-sm btn-primary" href="{{ path('app_main_new', {'id': users.id}) }}">Create a User</a>
                </td>

    <table class="table">

            <tbody>

                <h2>Membres de l'équipage</h2>

           {% for customer in users %}
                    <li>{{ customer.name}}</li>
                {% endfor %}

            </tbody>
    </table>
{% endblock %}
